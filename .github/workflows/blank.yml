name: CI/CD Workflow

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - closed

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
  
      - name: Ready for Review
        uses: jinmayamashita/ready-for-review@1.0.0
            
          
     
